# Spring Cloud Gateway

A minimal Spring Boot application using Spring Cloud Gateway.

## Run the application

```bash
mvn spring-boot:run
```

## Example request

```bash
curl http://localhost:8080/httpbin/get
```

This forwards requests to https://httpbin.org/get through the gateway.
